import Onboard from "@/components/Onboard";

export const metadata = {
  title: "Login/SignUp - Agent Bryan",
  description: "Agent Bryan",
};

const page = () => {
  return (
    <div>
      <Onboard />
    </div>
  );
};

export default page;
